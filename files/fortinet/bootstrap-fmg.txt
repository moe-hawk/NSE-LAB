# FortiManager bootstrap (FMG 7.2.2)

config system interface
  edit "port1"
    set ip 10.0.1.241 255.255.255.0
    set allowaccess ping http https ssh
  next
end

get system interface
get system status

# Manual checkpoints (not scriptable here):
# - Restore initial .dat config for your lab
# - Upload/activate VM license
